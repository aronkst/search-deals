<%= form_with(model: scraper) do |form| %>
  <% if scraper.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <h2><%= pluralize(scraper.errors.count, "error") %> prohibited this scraper from being saved:</h2>
      <ul>
        <% scraper.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-3 mb-1">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="mb-1">
    <%= form.label :url, class: "form-label" %>
    <%= form.text_field :url, class: "form-control" %>
  </div>

  <div class="mb-1">
    <%= form.label :pages, class: "form-label" %>
    <%= form.number_field :pages, class: "form-control" %>
  </div>

  <div class="mb-1">
    <%= form.label :value, class: "form-label" %>
    <%= form.number_field :value, class: "form-control" %>
  </div>

  <div class="mb-1">
    <%= form.label :porcentage, class: "form-label" %>
    <%= form.number_field :porcentage, class: "form-control" %>
  </div>

  <div class="my-3">
    <%= form.submit class: submit_class %>
  </div>
<% end %>
